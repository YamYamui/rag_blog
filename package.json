{
  "name": "rag-portfolio",
  "version": "1.0.0",
  "description": "Zero-backend client-side RAG portfolio",
  "type": "module",
  "scripts": {
    "build:index": "node scripts/build-index.mjs",
    "dev": "vite",
    "build": "npm run build:index && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@xenova/transformers": "^2.17.1"
  }
}
